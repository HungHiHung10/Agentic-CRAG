# modules/ingestion.py
import re
import os
import shutil
import unicodedata
from langchain_community.document_loaders import Docx2txtLoader
from langchain_core.documents import Document
from langchain_chroma import Chroma
from config import CHROMA_DB_DIR, FILES, CHUNK_SIZE, CHUNK_OVERLAP, USER_SOURCES_DIR



SOURCE_NAMES = {
    "noi_khoa_gs_chau": "Noi Khoa YHCT - GS Hoang Bao Chau",
    "benh_ngu_quan": "Benh Ngu Quan YHCT",
    "nhi_khoa": "Nhi Khoa YHCT",
    "noi_khoa_general": "noi-khoa-y-hoc-co-truyen",
}


def _clean_docx_text(text: str) -> str:
    text = (text or "").replace("</break>", " ").replace("<break>", " ")
    text = re.sub(r"\r\n?", "\n", text)
    text = re.sub(r"[ \t]+", " ", text)
    text = re.sub(r"\n{3,}", "\n\n", text)
    return text.strip()



SUPPORTED_USER_SOURCE_EXTS = {".docx", ".txt", ".md"}
DELETED_USER_SOURCES_FILE = USER_SOURCES_DIR / ".deleted_sources.txt"

def _deleted_user_source_names():
    if not DELETED_USER_SOURCES_FILE.exists():
        return set()
    try:
        return {
            line.strip()
            for line in DELETED_USER_SOURCES_FILE.read_text(encoding="utf-8", errors="ignore").splitlines()
            if line.strip()
        }
    except Exception:
        return set()


def _safe_filename(name: str) -> str:
    name = os.path.basename(name or "source")
    name = re.sub(r"[^A-Za-z0-9._ -]+", "_", name).strip(" ._")
    return name or "source"


def _read_plain_text_file(path: str) -> str:
    for encoding in ("utf-8-sig", "utf-8", "cp1258", "latin-1"):
        try:
            with open(path, "r", encoding=encoding) as handle:
                return handle.read()
        except UnicodeDecodeError:
            continue
    with open(path, "r", encoding="utf-8", errors="ignore") as handle:
        return handle.read()


def _load_source_text(path: str) -> str:
    ext = os.path.splitext(path)[1].lower()
    if ext == ".docx":
        return Docx2txtLoader(path).load()[0].page_content
    if ext in {".txt", ".md"}:
        return _read_plain_text_file(path)
    raise ValueError(f"Unsupported source file type: {ext}")


def _looks_like_heading(paragraph: str) -> bool:
    text = " ".join((paragraph or "").split()).strip()
    if not text or len(text) > 140:
        return False
    if text.endswith(('.', ',', ';')) and len(text) > 35:
        return False

    normalized = unicodedata.normalize("NFKC", text).lower()
    normalized = unicodedata.normalize("NFD", normalized)
    normalized = "".join(ch for ch in normalized if unicodedata.category(ch) != "Mn")
    if re.match(r"^(chuong|phan|muc|bai|quyen|[ivxlcdm]+\.|\d+(?:\.\d+)*\.)\s+", normalized):
        return True

    letters = [ch for ch in text if ch.isalpha()]
    if len(letters) >= 4:
        upper_ratio = sum(1 for ch in letters if ch.isupper()) / len(letters)
        if upper_ratio >= 0.7:
            return True

    return False


def _generic_split_text(text: str, source_id: str, title: str, splitter: str):
    raw_text = _clean_docx_text(text)
    paragraphs = [p.strip() for p in re.split(r"\n\s*\n|\n", raw_text) if p.strip()]
    if not paragraphs:
        return []

    chunks = []
    current = []
    current_len = 0
    current_section = title or source_id
    chunk_index = 1
    chunk_size = int(os.getenv("GENERIC_CHUNK_SIZE", str(CHUNK_SIZE)))
    overlap = max(0, int(os.getenv("GENERIC_CHUNK_OVERLAP", str(CHUNK_OVERLAP))))

    def add_chunk(content: str, section: str):
        nonlocal chunk_index
        content = content.strip()
        if not content:
            return
        chunks.append(Document(
            page_content=content,
            metadata={
                "source": source_id,
                "title": title,
                "section": section or f"Chunk {chunk_index}",
                "splitter": splitter,
                "chunk_index": chunk_index,
            },
        ))
        chunk_index += 1

    def flush():
        nonlocal current, current_len
        content = "\n\n".join(current).strip()
        add_chunk(content, current_section)
        if overlap > 0 and content:
            overlap_text = content[-overlap:].strip()
            current = [overlap_text] if overlap_text else []
            current_len = len(overlap_text)
        else:
            current = []
            current_len = 0

    for paragraph in paragraphs:
        if _looks_like_heading(paragraph):
            if current:
                flush()
            current_section = paragraph.strip()
            current = [paragraph]
            current_len = len(paragraph)
            continue

        if len(paragraph) > chunk_size:
            if current:
                flush()
            start = 0
            step = max(1, chunk_size - overlap)
            while start < len(paragraph):
                add_chunk(paragraph[start:start + chunk_size], current_section)
                start += step
            continue

        projected_len = current_len + len(paragraph) + 2
        if current and projected_len > chunk_size:
            flush()

        current.append(paragraph)
        current_len += len(paragraph) + 2

    if current:
        flush()

    return chunks
def _generic_split_docx(file_key: str, source_id: str):
    """Fallback splitter used when the hand-written book parser returns no chunks."""
    path = FILES[file_key]
    if not os.path.exists(path):
        print(f"[WARNING] File not found for fallback splitter: {path}")
        return []

    print(f"[INFO] Using generic fallback splitter for {source_id}.")
    raw_text = _load_source_text(path)
    return _generic_split_text(
        text=raw_text,
        source_id=source_id,
        title=SOURCE_NAMES.get(file_key, source_id),
        splitter="generic_fallback",
    )


def split_user_sources():
    USER_SOURCES_DIR.mkdir(parents=True, exist_ok=True)
    chunks = []
    for path_obj in sorted(USER_SOURCES_DIR.iterdir(), key=lambda item: item.name.lower()):
        if not path_obj.is_file() or path_obj.name.startswith("~$") or path_obj.name == "test-user-source.txt" or path_obj.name in _deleted_user_source_names():
            continue

        ext = path_obj.suffix.lower()
        if ext not in SUPPORTED_USER_SOURCE_EXTS:
            print(f"[WARNING] Skip unsupported uploaded source: {path_obj.name}")
            continue

        try:
            print(f"[PROCESS] Processing uploaded source: {path_obj.name}")
            raw_text = _load_source_text(str(path_obj))
            source_id = f"user:{path_obj.name}"
            source_chunks = _generic_split_text(
                text=raw_text,
                source_id=source_id,
                title=path_obj.name,
                splitter="user_upload",
            )
            print(f"[INFO] {source_id}: {len(source_chunks)} chunks")
            chunks.extend(source_chunks)
        except Exception as exc:
            print(f"[WARNING] Could not process uploaded source {path_obj.name}: {exc}")

    return chunks


def list_source_inventory():
    rows = []
    for file_key, path in FILES.items():
        if file_key in {"eval_json", "predictions"}:
            continue
        exists = os.path.exists(path)
        rows.append({
            "kind": "Built-in",
            "name": SOURCE_NAMES.get(file_key, file_key),
            "path": str(path),
            "status": "Available" if exists else "Missing",
        })

    USER_SOURCES_DIR.mkdir(parents=True, exist_ok=True)
    for path_obj in sorted(USER_SOURCES_DIR.iterdir(), key=lambda item: item.name.lower()):
        if not path_obj.is_file() or path_obj.name.startswith("~$") or path_obj.name == "test-user-source.txt" or path_obj.name in _deleted_user_source_names():
            continue
        status = "Available" if path_obj.suffix.lower() in SUPPORTED_USER_SOURCE_EXTS else "Unsupported"
        rows.append({
            "kind": "Uploaded",
            "name": path_obj.name,
            "path": str(path_obj),
            "status": status,
        })
    return rows


def _split_with_fallback(file_key: str, source_id: str, splitter):
    docs = splitter()
    if docs:
        return docs
    return _generic_split_docx(file_key, source_id)

def split_noi_khoa_gs_chau():
    print("[PROCESS] Processing Noi Khoa YHCT - GS Hoang Bao Chau...")
    path = FILES["noi_khoa_gs_chau"]
    
    if not os.path.exists(path):
        print(f"[WARNING] File not found: {path}")
        return []

    loader = Docx2txtLoader(path)
    raw_text = loader.load()[0].page_content
    raw_text = re.sub(r'\n*\s*_{8,}\s*\n*', '\n__________\n', raw_text)
    parts = re.split(r'\n__________\n', raw_text)

    docs = []
    for i, part in enumerate(parts):
        part = part.strip()
        if not part: continue
        lines = [line.strip() for line in part.split('\n') if line.strip()]
        title_line = lines[0] if lines else f"Untitled {i}"
        for line in lines:
            if line.isupper() or (len(line) > 5 and line == line.upper()):
                title_line = line; break
        docs.append(Document(page_content=part, metadata={"title": title_line.strip()}))

    PATTERNS = [r'^[A-Z]\.\s', r'^\d+\.\s', r'^\d+\.\d+\.\s', r'^[a-zÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¹Ã…â€œ]\.\s']

    def recursive_split_gs(lines, current_title, current_section, pattern_index):
        if pattern_index >= len(PATTERNS):
            content = '\n'.join(lines).strip()
            if not content: return []
            return [Document(
                page_content=content,
                metadata={
                    "source": "Noi_Khoa_YHCT_GS_Hoang_Bao_Chau",
                    "title": current_title,
                    "section": current_section
                }
            )]

        current_pattern = PATTERNS[pattern_index]
        splits = []
        for i, line in enumerate(lines):
            if re.match(current_pattern, line.strip()):
                splits.append((i, line.strip()))

        if not splits:
            return recursive_split_gs(lines, current_title, current_section, pattern_index + 1)

        results = []
        splits = [(0, None)] + splits + [(len(lines), None)]
        splits.sort(key=lambda x: x[0])

        for j in range(len(splits) - 1):
            start_idx, header = splits[j]
            end_idx = splits[j + 1][0]
            sub_lines = lines[start_idx + 1 : end_idx]

            if header is None:
                if not sub_lines: continue
                suffix = " (DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p)" if "DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p" not in current_section else ""
                new_section_name = f"{current_section}{suffix}"
                results.extend(recursive_split_gs(sub_lines, current_title, new_section_name, pattern_index + 1))
            else:
                new_section_name = f"{current_section} - {header}"
                content_for_next_level = [header] + sub_lines
                results.extend(recursive_split_gs(content_for_next_level, current_title, new_section_name, pattern_index + 1))
        return results

    chunks = []
    main_headers = ["ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â I CÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â NG", "CHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨NG TRÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â ", "NGUYÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â N NHÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡N"]

    for doc in docs:
        part = doc.page_content.strip()
        title = doc.metadata.get("title", "")
        if not part: continue
        lines = part.split('\n')
        
        splits = []
        for i, line in enumerate(lines):
            clean_header = line.strip().rstrip(':')
            if clean_header in main_headers:
                splits.append((i, clean_header))
        
        splits = [(0, None)] + splits + [(len(lines), None)]
        splits.sort(key=lambda x: x[0])

        for j in range(len(splits) - 1):
            start_index, header = splits[j]
            end_index = splits[j + 1][0]
            if header is None: continue
            
            section_lines = lines[start_index + 1 : end_index]
            if not section_lines: continue
            
            sub_docs = recursive_split_gs(section_lines, title, header, pattern_index=0)
            for sub_doc in sub_docs:
                Content = f"{header}\n\n{sub_doc.page_content}"
                sub_doc.page_content = Content
                chunks.append(sub_doc)
                
    return chunks

def split_benh_ngu_quan():
    print("[PROCESS] Processing Benh Ngu Quan...")
    file_path = FILES["benh_ngu_quan"]
    
    if not os.path.exists(file_path):
        print(f"[WARNING] File not found: {file_path}")
        return []

    BOOK_NAME = "bÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¡nh ngÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â¦ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â© quan"
    MARKER_START = "CHÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂN (9) ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂIÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬U CÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡N DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶N"
    NO_SPLIT_TITLES = ["CHÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂN (9) ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂIÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬U CÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡N DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶N", "LÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œI NÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“I ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦U"]

    loader = Docx2txtLoader(file_path)
    raw_text = loader.load()[0].page_content
    raw_text = raw_text.replace('</break>', ' ').replace('<break>', ' ')
    raw_text = re.sub(r'\n*\s*_{8,}\s*\n*', '\n__________\n', raw_text)
    parts = re.split(r'\n__________\n', raw_text)

    SUB_PATTERNS = [
        r'^\s*(?:PhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â§n|ChÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¡ng)?\s*[IVX]+\.[\s\t]*',
        r'^\s*\d+\.[\s\t]*',
        r'^\s*\d+\.\d+\.[\s\t]*',
        r'^\s*[a-zÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¹Ã…â€œ]\.[\s\t]*'
    ]

    def recursive_split_nq(lines, current_title, current_section_name, pattern_idx):
        def create_doc(content_lines, section_override=None):
            content = '\n'.join(content_lines).strip()
            if not content: return None
            final_section = section_override if section_override else current_section_name
            full_content = f"{current_title}-{final_section}\nNÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“I DUNG:\n{content}"
            return Document(
                page_content=full_content,
                metadata={"source": BOOK_NAME, "title": current_title, "section": final_section}
            )

        if pattern_idx >= len(SUB_PATTERNS):
            doc = create_doc(lines)
            return [doc] if doc else []

        current_pattern = SUB_PATTERNS[pattern_idx]
        splits = []

        for i, line in enumerate(lines):
            line_strip = line.strip()
            is_match = False
            if re.match(current_pattern, line_strip): is_match = True
            
            if not is_match and pattern_idx == 0:
                if any(line_strip.upper().startswith(k) for k in ["PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ LÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤C", "PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ PHÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â NG"]): is_match = True
            
            if not is_match and pattern_idx == 1:
                keywords_rescue = ["PhÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â©p chÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯a", "PhÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¡ng phÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¡p", "ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂiÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Âu trÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¹", "TriÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¡u chÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â©ng"]
                if any(line_strip.startswith(k) for k in keywords_rescue) and len(line_strip) < 50: is_match = True

            if is_match: splits.append((i, line_strip))

        if not splits:
            if pattern_idx == 0:
                doc = create_doc(lines, section_override="TÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ng quan")
                return [doc] if doc else []
            else:
                return recursive_split_nq(lines, current_title, current_section_name, pattern_idx + 1)

        results = []
        splits = [(0, None)] + splits + [(len(lines), None)]
        splits.sort(key=lambda x: x[0])

        for j in range(len(splits) - 1):
            start_idx, header = splits[j]
            end_idx = splits[j + 1][0]
            sub_lines = lines[start_idx + 1 : end_idx]
            if not sub_lines: continue

            if header is None:
                suffix = " (DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p)" if "DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p" not in current_section_name else ""
                final_sec = f"{current_section_name}{suffix}" if current_section_name else "TÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ng quan"
                doc = create_doc(sub_lines, section_override=final_sec)
                if doc: results.append(doc)
            else:
                new_section = header if not current_section_name else f"{current_section_name} > {header}"
                special_list = ["PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ LÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤C", "PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ PHÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â NG", "BÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬I ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢C THÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â M"]
                if any(k in header.upper() for k in special_list):
                    results.extend(recursive_split_nq(sub_lines, current_title, new_section, 0))
                else:
                    content_next = [header] + sub_lines
                    results.extend(recursive_split_nq(content_next, current_title, new_section, pattern_idx + 1))
        return results

    chunks = []
    
    def process_buffer(lines, title):
        if not lines: return
        if any(k in title.upper() for k in NO_SPLIT_TITLES):
            full = f"{title}-ToÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â n vÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢n\nNÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“I DUNG:\n" + "\n".join(lines)
            chunks.append(Document(page_content=full, metadata={"source": BOOK_NAME, "title": title, "section": "ToÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â n vÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢n"}))
            return
        chunks.extend(recursive_split_nq(lines, title, "", pattern_idx=0))

    is_phase_2 = False
    pre_content = []
    active_title = "TÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ng quan"
    last_processed_title = "TÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ng quan"
    current_disease_buffer = []

    all_lines = []
    for part in parts:
        all_lines.extend([line.strip() for line in part.split('\n') if line.strip()])

    for i, line in enumerate(all_lines):
        line_strip = line.strip()
        if not line_strip: continue

        if not is_phase_2:
            if MARKER_START in line_strip.upper() and not re.search(r'\d+$', line_strip):
                is_phase_2 = True
                if pre_content:
                    chunks.append(Document(page_content=f"PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦N MÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¾ ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦U / MÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤C LÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤C\n\n" + "\n".join(pre_content), 
                                           metadata={"source": BOOK_NAME, "title": BOOK_NAME, "section": "Intro"}))
                active_title = line_strip
                last_processed_title = line_strip
                current_disease_buffer = []
                continue
            pre_content.append(line_strip)
            continue

        is_upper = (line_strip.isupper() and len(line_strip) > 5
                    and not re.match(SUB_PATTERNS[0], line_strip)
                    and not any(bad in line_strip.upper() for bad in ["TRÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œNG ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â I HÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢C", "KHOA Y HÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢C", "BÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“ MÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂN", "BÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â NH NGÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â¦ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ QUAN"]))
        is_appendix = any(k in line_strip.upper() for k in ["PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ LÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤C", "PHÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ PHÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â NG"])

        if is_upper:
            process_buffer(current_disease_buffer, active_title)
            current_disease_buffer = []
            if is_appendix:
                active_title = f"{last_processed_title} - {line_strip}"
            else:
                active_title = line_strip
                last_processed_title = line_strip
            continue
        current_disease_buffer.append(line_strip)

    process_buffer(current_disease_buffer, active_title)
    return chunks

def split_nhi_khoa():
    print("[PROCESS] Processing Nhi Khoa YHCT...")
    file_path = FILES["nhi_khoa"]
    
    if not os.path.exists(file_path):
        print(f"[WARNING] File not found: {file_path}")
        return []

    raw_text = Docx2txtLoader(file_path).load()[0].page_content

    def is_skip(line: str) -> bool:
        line = line.strip()
        return (not line or line.lower() == "</break>" or re.fullmatch(r"_+", line) is not None)

    lines = [line.strip() for line in raw_text.split("\n") if not is_skip(line)]

    CHAPTER_PATTERN = re.compile(r"^CHÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â NG\s+([IVXLC]+)", re.I)
    LESSON_PATTERN  = re.compile(r"^BÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â i\s+\d+", re.I)
    ROMAN_HEADER    = re.compile(r"^[IVX]+\.\s+.+")
    NUMBER_HEADER   = re.compile(r"^(\d+(?:\.\d+)*\.)\s+(.+)")

    def parse_number_path(header: str):
        nums = header.split()[0].strip(".")
        return [int(x) for x in nums.split(".")]

    chunks = []
    current_chapter = None
    in_chapter = False
    current_lesson = None
    current_title = None
    section_stack = []
    content_buffer = []

    def flush_buffer():
        if not content_buffer or not section_stack: return
        full_section = " > ".join(h for _, h in section_stack)
        leaf_header = section_stack[-1][1]
        chunks.append(Document(
            page_content="\n".join([leaf_header] + content_buffer).strip(),
            metadata={
                "source": "nhi-khoa-y-hoc-co-truyen",
                "title": current_title,
                "section": full_section
            }
        ))
        content_buffer.clear()

    for line in lines:
        chap_match = CHAPTER_PATTERN.match(line)
        if chap_match:
            flush_buffer()
            current_chapter = chap_match.group(1)
            in_chapter = True
            current_lesson = None; current_title = None; section_stack.clear()
            continue

        if not in_chapter: continue

        if LESSON_PATTERN.match(line):
            flush_buffer()
            section_stack.clear()
            current_lesson = line
            current_title = None
            continue

        if current_lesson and not current_title and line.isupper():
            current_title = line
            continue

        if not current_title: continue

        if ROMAN_HEADER.match(line):
            flush_buffer()
            section_stack = [(1, line)]
            continue

        num_match = NUMBER_HEADER.match(line)
        if num_match:
            if current_chapter == "I": continue
            flush_buffer()
            level = len(parse_number_path(line)) + 1
            while section_stack and section_stack[-1][0] >= level:
                section_stack.pop()
            section_stack.append((level, line))
            continue

        if section_stack:
            content_buffer.append(line)

    flush_buffer()
    return chunks

def split_noi_khoa_general():
    print("[PROCESS] Processing Noi Khoa General...")
    file_path = FILES["noi_khoa_general"]
    
    if not os.path.exists(file_path):
        print(f"[WARNING] File not found: {file_path}")
        return []

    loader = Docx2txtLoader(file_path)
    raw_text = loader.load()[0].page_content
    chapter_pattern = r'(?m)^(?![0-9]+\.|I+\.|V\.)(?![a-z])([A-ZÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¹ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â¦ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂªÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â®ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â²ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â´ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¸ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¼ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¾ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â½ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¾ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂªÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â®ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â²ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â´ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¸\s,()-]{3,})$'
    raw_text_processed = re.sub(chapter_pattern, r'\n__________\n\1', raw_text)
    parts = re.split(r'\n__________\n', raw_text_processed)

    docs_by_section = []
    for part in parts:
        part = part.strip()
        lines = [line.strip() for line in part.split('\n') if line.strip()]
        if not lines or len(part) < 50: continue
        title_line = lines[0]
        docs_by_section.append(Document(page_content=part, metadata={"title": title_line}))

    SUB_PATTERNS = [
        r'^\d+\.\s+[A-ZÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¹ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã¢â‚¬Å“ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â¦ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂªÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â®ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â²ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â´ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¸ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¼ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¾ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹ÃƒÂ¢Ã¢â€šÂ¬Ã‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â½ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â‚¬Å¾Ã‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¹Ãƒâ€¦Ã¢â‚¬Å“ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦ÃƒÂ¢Ã¢â€šÂ¬Ã…â€œÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Â¦Ãƒâ€šÃ‚Â¾ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¢ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¦ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¨ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂªÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¬ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â®ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â°ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â²ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â´ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¶ÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â»ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¸]',
        r'^\d+\.\d+\.\s',
        r'^\d+\.\d+\.\d+\.\s',
        r'^[a-zÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¾ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¹Ã…â€œ]\.\s',
    ]

    def recursive_split_gen(lines, current_title, current_section_name, pattern_idx):
        if pattern_idx >= len(SUB_PATTERNS):
            content = '\n'.join(lines).strip()
            if not content: return []
            return [Document(
                page_content=content,
                metadata={
                    "disease": current_title,
                    "section": current_section_name,
                    "source": "noi-khoa-y-hoc-co-truyen"
                }
            )]

        current_pattern = SUB_PATTERNS[pattern_idx]
        splits = []
        for i, line in enumerate(lines):
            if re.match(current_pattern, line.strip()):
                splits.append((i, line.strip()))

        if not splits:
            return recursive_split_gen(lines, current_title, current_section_name, pattern_idx + 1)

        results = []
        splits = [(0, None)] + splits + [(len(lines), None)]

        for j in range(len(splits) - 1):
            start_idx, header = splits[j]
            end_idx = splits[j + 1][0]
            sub_lines = lines[start_idx + 1 : end_idx]

            if header is None:
                if not sub_lines: continue
                suffix = " (DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p)" if "DÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â«n nhÃƒÆ’Ã†â€™Ãƒâ€šÃ‚Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚ÂºÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â­p" not in current_section_name else ""
                new_section_name = f"{current_section_name}{suffix}"
                results.extend(recursive_split_gen(sub_lines, current_title, new_section_name, pattern_idx + 1))
            else:
                new_section_name = f"{current_section_name} > {header}"
                content_for_next_level = [header] + sub_lines
                results.extend(recursive_split_gen(content_for_next_level, current_title, new_section_name, pattern_idx + 1))
        return results

    refined_docs = []
    for doc in docs_by_section:
        part = doc.page_content.strip()
        title = doc.metadata.get("title", "")
        lines = part.split('\n')
        refined_docs.extend(recursive_split_gen(lines, title, title, pattern_idx=0))

    chunks = [d for d in refined_docs if "</break>" not in d.metadata.get('disease', '')]
    return chunks


class SimpleVectorDB:
    def __init__(self, documents, embeddings, embedding_model):
        self.documents = documents
        self.embeddings = embeddings
        self.embedding_model = embedding_model

    @classmethod
    def from_documents(cls, documents, embedding_model):
        texts = [doc.page_content for doc in documents]
        embeddings = embedding_model.embed_documents(texts)
        return cls(documents, embeddings, embedding_model)

    def as_retriever(self, search_type=None, search_kwargs=None):
        return SimpleRetriever(self.documents, self.embeddings, self.embedding_model, search_kwargs or {})


class SimpleRetriever:
    STOPWORDS = {
        "a", "an", "and", "are", "as", "in", "is", "of", "on", "or", "the", "to", "what",
        "answer", "english", "only", "question", "please",
        "la", "lÃ ", "gi", "gÃ¬", "nghia", "nghÄ©a", "cho", "hoi", "há»i", "ve", "vá»",
        "hay", "nhung", "nhá»¯ng", "cac", "cÃ¡c", "mot", "má»™t", "benh", "bá»‡nh", "trieu", "triá»‡u",
        "chung", "chá»©ng", "cach", "cÃ¡ch", "dieu", "Ä‘iá»u", "tri", "trá»‹", "va", "vÃ ",
    }
    def __init__(self, documents, embeddings, embedding_model, search_kwargs):
        self.documents = documents
        self.embeddings = embeddings
        self.embedding_model = embedding_model
        self.k = int(search_kwargs.get("k", 3))
        self.score_threshold = float(search_kwargs.get("score_threshold", -1.0))
        self.normalized_documents = [self._document_text(doc) for doc in documents]

    @staticmethod
    def _dot(left, right):
        return sum(a * b for a, b in zip(left, right))

    @staticmethod
    def _normalize(text):
        text = unicodedata.normalize("NFKC", text or "").lower()
        text = unicodedata.normalize("NFD", text)
        text = "".join(ch for ch in text if unicodedata.category(ch) != "Mn")
        text = text.replace("Ä‘", "d")
        return re.sub(r"\s+", " ", text).strip()

    @classmethod
    def _tokens(cls, text):
        normalized = cls._normalize(text)
        return [tok for tok in re.findall(r"\w+", normalized, flags=re.UNICODE) if tok not in cls.STOPWORDS]

    @classmethod
    def _query_phrase(cls, question):
        question = re.sub(r"answer\s+in\s+english\s+only\.?", " ", question or "", flags=re.I)
        tokens = cls._tokens(question)
        ngrams = []
        for i in range(len(tokens) - 1):
            ngrams.append(" ".join(tokens[i:i+2]))
        for i in range(len(tokens) - 2):
            ngrams.append(" ".join(tokens[i:i+3]))
        return ngrams, tokens

    @classmethod
    def _document_text(cls, doc):
        metadata = getattr(doc, "metadata", {}) or {}
        metadata_text = " ".join(str(value) for value in metadata.values())
        return cls._normalize(f"{metadata_text}\n{getattr(doc, 'page_content', '')}")

    @classmethod
    def _lexical_score(cls, question, normalized_doc):
        ngrams, tokens = cls._query_phrase(question)
        if not tokens:
            return 0.0

        score = 0.0
        # High reward for exact multi-word medical terms
        for phrase in ngrams:
            if phrase in normalized_doc:
                score += 25.0

        matched = 0
        for token in tokens:
            count = normalized_doc.count(token)
            if count:
                matched += 1
                score += min(count, 4) * 1.5

        if matched == len(tokens) and len(tokens) > 0:
            score += 10.0

        return score

    @staticmethod
    def _source_id(doc):
        metadata = getattr(doc, "metadata", {}) or {}
        return str(metadata.get("source") or metadata.get("title") or "<unknown>")

    def _diversify_sources(self, scored_items):
        if os.getenv("DIVERSIFY_SOURCES", "true").strip().lower() not in {"1", "true", "yes", "on"}:
            return [doc for *_, doc in scored_items[: self.k]]

        selected = []
        selected_ids = set()
        max_unique = min(self.k, int(os.getenv("MIN_SOURCE_DIVERSITY", "3")))
        min_strong_lexical = float(os.getenv("DIVERSITY_MIN_LEXICAL", "25"))
        strong_items = [item for item in scored_items if item[1] >= min_strong_lexical]
        diversity_pool = strong_items or scored_items

        for item in diversity_pool:
            doc = item[-1]
            source_id = self._source_id(doc)
            if source_id in selected_ids:
                continue
            selected.append(doc)
            selected_ids.add(source_id)
            if len(selected) >= max_unique:
                break

        for item in scored_items:
            doc = item[-1]
            if doc in selected:
                continue
            selected.append(doc)
            if len(selected) >= self.k:
                break

        return selected[: self.k]
    def invoke(self, question):
        query_embedding = self.embedding_model.embed_query(question)
        scored = []
        for embedding, doc, normalized_doc in zip(self.embeddings, self.documents, self.normalized_documents):
            vector_score = self._dot(query_embedding, embedding)
            lexical_score = self._lexical_score(question, normalized_doc)
            total_score = (lexical_score * 10.0) + vector_score
            scored.append((total_score, lexical_score, vector_score, doc))

        scored.sort(key=lambda item: item[0], reverse=True)
        lexical_hits = [item for item in scored if item[1] > 0]
        if lexical_hits:
            return self._diversify_sources(lexical_hits)

        filtered = [item for item in scored if item[0] >= self.score_threshold]
        if not filtered:
            filtered = scored[: self.k]
        return self._diversify_sources(filtered)

def create_vector_db(embedding_model, force_rebuild=False):
    use_chroma = os.getenv("VECTOR_STORE_BACKEND", "memory").strip().lower() == "chroma"

    if use_chroma and os.path.exists(CHROMA_DB_DIR) and not force_rebuild:
        try:
            print(f"[INFO] ChromaDB already exists at {CHROMA_DB_DIR}. Loading...")
            return Chroma(
                persist_directory=CHROMA_DB_DIR,
                embedding_function=embedding_model,
                collection_name="y_hoc_co_truyen",
            )
        except Exception as exc:
            print(f"[WARNING] Failed to load ChromaDB, rebuilding in memory: {exc}")
            use_chroma = False

    if use_chroma and force_rebuild and os.path.exists(CHROMA_DB_DIR):
        try:
            shutil.rmtree(CHROMA_DB_DIR)
            print("[SUCCESS] Removed old Chroma directory")
        except Exception as exc:
            print(f"[WARNING] Could not remove old Chroma directory: {exc}")

    splitters = [
        ("noi_khoa_gs_chau", "Noi_Khoa_YHCT_GS_Hoang_Bao_Chau", split_noi_khoa_gs_chau),
        ("benh_ngu_quan", "Benh_Ngu_Quan_YHCT", split_benh_ngu_quan),
        ("nhi_khoa", "Nhi_Khoa_YHCT", split_nhi_khoa),
        ("noi_khoa_general", "noi-khoa-y-hoc-co-truyen", split_noi_khoa_general),
    ]

    all_chunks = []
    for file_key, source_id, splitter in splitters:
        docs = _split_with_fallback(file_key, source_id, splitter)
        print(f"[INFO] {source_id}: {len(docs)} chunks")
        all_chunks.extend(docs)

    user_chunks = split_user_sources()
    all_chunks.extend(user_chunks)

    if not all_chunks:
        print("[WARNING] No chunks created. Check file paths.")
        return None

    print(f"[PROCESS] Creating Vector DB with {len(all_chunks)} chunks...")
    if use_chroma:
        try:
            chroma_db = Chroma.from_documents(
                documents=all_chunks,
                collection_name="y_hoc_co_truyen",
                embedding=embedding_model,
                collection_metadata={"hnsw:space": "cosine"},
                persist_directory=CHROMA_DB_DIR,
            )
            print(f"[SUCCESS] Vector DB Created with count: {chroma_db._collection.count()}")
            return chroma_db
        except Exception as exc:
            print(f"[WARNING] ChromaDB unavailable, using in-memory vector store: {exc}")

    simple_db = SimpleVectorDB.from_documents(all_chunks, embedding_model)
    print(f"[SUCCESS] In-memory Vector DB Created with count: {len(all_chunks)}")
    return simple_db
